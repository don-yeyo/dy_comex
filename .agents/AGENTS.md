# Reglas del Proyecto

- No ejecutar ni solicitar ejecución de comandos de build (`npm run build`, `npm --prefix client run build` o similares) durante el desarrollo, ya que se utiliza Hot Reload en tiempo real.
